// MAP :
    // {
    //     // Using map we can iterate over each element ,modify it and return a new array of the modified elements 
    //     let myArr = [1,2,3,4]
    //     newArr = myArr.map((element,index) => {
    //         // console.log("element is ",element,"index is ",index)
    //         return element*2
    //     })
    //     console.log("new Array is ",newArr)

    // }
